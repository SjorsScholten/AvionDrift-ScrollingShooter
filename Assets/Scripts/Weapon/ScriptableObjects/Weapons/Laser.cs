using UnityEngine;

namespace Entities {
    public class Laser : BaseWeapon {
        public override void Fire(Vector3 position, Element element, float deltaTime) {
            //raycast with line-renderer
        }
    }
}